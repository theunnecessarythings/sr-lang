package libc

// Derived from musl-1.2.5/src/stat/fstat.c (syscall wrapper).

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

fstat :: proc(fd: i32, buf: *stat) i32 {
    r := sc.syscall2_ret(nums.SYS_fstat.(i64), fd.(i64), buf.^usize.(i64))
    return r.(i32)
}
