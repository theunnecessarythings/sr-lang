package libc

// Derived from musl-1.2.5/src/stat/umask.c (syscall wrapper).

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

umask :: proc(mode: mode_t) mode_t {
    r := sc.syscall1_ret(nums.SYS_umask.(i64), mode.(i64))
    return r.(mode_t)
}
