package libc

// Derived from musl-1.2.5/src/thread/synccall.c usage (syscall wrapper).

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

gettid :: proc() i32 {
    r := sc.syscall0_ret(nums.SYS_gettid.(i64))
    return r.(i32)
}
