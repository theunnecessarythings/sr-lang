package libc

// Derived from musl-1.2.5/src/unistd/acct.c

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

acct :: proc(path: *const u8) i32 {
    r := sc.syscall1_ret(nums.SYS_acct.(i64), path.^usize.(i64))
    return r.(i32)
}
