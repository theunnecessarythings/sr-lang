package libc

// Derived from musl-1.2.5/src/process/fork.c (syscall wrapper).

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

fork :: proc() i32 {
    r := sc.syscall0_ret(nums.SYS_fork.(i64))
    return r.(i32)
}
