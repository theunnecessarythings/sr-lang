package libc

// Derived from musl-1.2.5/src/unistd/setpgid.c

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

setpgid :: proc(pid: i32, pgid: i32) i32 {
    r := sc.syscall2_ret(nums.SYS_setpgid.(i64), pid.(i64), pgid.(i64))
    return r.(i32)
}
