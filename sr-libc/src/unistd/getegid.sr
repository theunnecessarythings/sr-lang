package libc

// Derived from musl-1.2.5/src/unistd/getegid.c (syscall wrapper).

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

getegid :: proc() i32 {
    r := sc.syscall0_ret(nums.SYS_getegid.(i64))
    return r.(i32)
}
