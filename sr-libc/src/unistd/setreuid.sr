package libc

// Derived from musl-1.2.5/src/unistd/setreuid.c

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

setreuid :: proc(ruid: uid_t, euid: uid_t) i32 {
    r := sc.syscall2_ret(nums.SYS_setreuid.(i64), ruid.(i64), euid.(i64))
    return r.(i32)
}
