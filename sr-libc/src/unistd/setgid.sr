package libc

// Derived from musl-1.2.5/src/unistd/setgid.c

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

setgid :: proc(gid: gid_t) i32 {
    r := sc.syscall1_ret(nums.SYS_setgid.(i64), gid.(i64))
    return r.(i32)
}
