package libc

// Derived from musl-1.2.5/src/unistd/posix_close.c

close_mod :: import "libc/src/unistd/close"

posix_close :: proc(fd: i32, flags: i32) i32 {
    _ = flags
    return close_mod.close(fd)
}
