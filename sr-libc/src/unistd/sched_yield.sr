package libc

// Derived from musl-1.2.5/src/thread/thrd_yield.c (syscall wrapper).

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

sched_yield :: proc() i32 {
    r := sc.syscall0_ret(nums.SYS_sched_yield.(i64))
    return r.(i32)
}
