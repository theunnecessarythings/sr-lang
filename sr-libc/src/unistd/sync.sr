package libc

// Derived from musl-1.2.5/src/unistd/sync.c

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

sync :: proc() void {
    _ = sc.syscall0_ret(nums.SYS_sync.(i64))
}
