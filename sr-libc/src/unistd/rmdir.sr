package libc

// Derived from musl-1.2.5/src/unistd/rmdir.c (syscall wrapper).

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

rmdir :: proc(path: *const u8) i32 {
    r := sc.syscall1_ret(nums.SYS_rmdir.(i64), path.^usize.(i64))
    return r.(i32)
}
