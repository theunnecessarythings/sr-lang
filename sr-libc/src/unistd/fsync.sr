package libc

// Derived from musl-1.2.5/src/unistd/fsync.c

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

fsync :: proc(fd: i32) i32 {
    r := sc.syscall1_ret(nums.SYS_fsync.(i64), fd.(i64))
    return r.(i32)
}
