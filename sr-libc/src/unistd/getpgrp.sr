package libc

// Derived from musl-1.2.5/src/unistd/getpgrp.c (syscall wrapper).

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

getpgrp :: proc() i32 {
    r := sc.syscall0_ret(nums.SYS_getpgrp.(i64))
    return r.(i32)
}
