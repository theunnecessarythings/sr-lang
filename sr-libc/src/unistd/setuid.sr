package libc

// Derived from musl-1.2.5/src/unistd/setuid.c

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

setuid :: proc(uid: uid_t) i32 {
    r := sc.syscall1_ret(nums.SYS_setuid.(i64), uid.(i64))
    return r.(i32)
}
