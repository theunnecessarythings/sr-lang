package libc

// Derived from musl-1.2.5/src/unistd/fdatasync.c

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

fdatasync :: proc(fd: i32) i32 {
    r := sc.syscall1_ret(nums.SYS_fdatasync.(i64), fd.(i64))
    return r.(i32)
}
