package libc

// Derived from musl-1.2.5/src/unistd/dup2.c (syscall wrapper).

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

dup2 :: proc(oldfd: i32, newfd: i32) i32 {
    r := sc.syscall2_ret(nums.SYS_dup2.(i64), oldfd.(i64), newfd.(i64))
    return r.(i32)
}
