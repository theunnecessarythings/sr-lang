package libc

// Derived from musl-1.2.5/src/unistd/pause.c

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

pause :: proc() i32 {
    r := sc.syscall0_ret(nums.SYS_pause.(i64))
    return r.(i32)
}
