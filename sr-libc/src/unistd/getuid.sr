package libc

// Derived from musl-1.2.5/src/unistd/getuid.c (syscall wrapper).

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

getuid :: proc() i32 {
    r := sc.syscall0_ret(nums.SYS_getuid.(i64))
    return r.(i32)
}
