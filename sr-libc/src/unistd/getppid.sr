package libc

// Derived from musl-1.2.5/src/unistd/getppid.c (syscall wrapper).

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

getppid :: proc() i32 {
    r := sc.syscall0_ret(nums.SYS_getppid.(i64))
    return r.(i32)
}
