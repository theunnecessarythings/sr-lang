package libc

// Derived from musl-1.2.5/src/string/strnlen.c.

memchr_mod :: import "libc/src/string/memchr"

strnlen :: proc(s: *const u8, n: usize) usize {
    p := memchr_mod.memchr(s, 0, n)
    if p != null {
        return (p?.^usize - s.^usize).(usize)
    }
    return n
}
