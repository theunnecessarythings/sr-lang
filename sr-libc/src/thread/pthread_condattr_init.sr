package libc

// Derived from musl-1.2.5/src/thread/pthread_condattr_init.c

string_h :: import "libc/src/string"

pthread_condattr_init :: proc(a: *pthread_condattr_t) i32 {
    _ = string_h.memset(a.^*void, 0, sizeof(pthread_condattr_t))
    return 0
}
