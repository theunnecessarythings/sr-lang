package libc

// Derived from musl-1.2.5/src/thread/pthread_attr_setstacksize.c

attr_int :: import "libc/src/thread/pthread_attr_internal"
errno_mod :: import "libc/include/errno"

pthread_attr_setstacksize :: proc(a: *alltypes.pthread_attr_t, size: usize) i32 {
    if (size - attr_int.PTHREAD_STACK_MIN.(usize)) > ((~0).(usize) / 4) { return errno_mod.EINVAL }
    attr_int.attr_stackaddr(a).* = 0
    attr_int.attr_stacksize(a).* = size
    return 0
}
