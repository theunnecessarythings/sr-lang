package libc

// Derived from musl-1.2.5/src/thread/pthread_attr_setschedpolicy.c

attr_int :: import "libc/src/thread/pthread_attr_internal"

pthread_attr_setschedpolicy :: proc(a: *alltypes.pthread_attr_t, policy: i32) i32 {
    attr_int.attr_policy(a).* = policy
    return 0
}
