package libc

// Derived from musl-1.2.5/src/misc/getpriority.c

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

getpriority :: proc(which: i32, who: id_t) i32 {
    r := sc.syscall2_ret(nums.SYS_getpriority.(i64), which.(i64), who.(i64))
    return r.(i32)
}
