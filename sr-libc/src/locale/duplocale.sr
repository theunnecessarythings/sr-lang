package libc

// Derived from musl-1.2.5/src/locale/duplocale.c

loc_impl :: import "libc/src/locale/locale_impl"

__duplocale :: proc(old: locale_t) locale_t {
    if old == null {
        return loc_impl.__current_locale
    }
    return old
}
