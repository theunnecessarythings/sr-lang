package libc

// Derived from musl-1.2.5/src/signal/sigwaitinfo.c

sigtimedwait_mod :: import "libc/src/signal/sigtimedwait"

sigwaitinfo :: proc(mask: *const alltypes.sigset_t, si: *siginfo_t) i32 {
    return sigtimedwait_mod.sigtimedwait(mask, si, null)
}
