package libc

// Derived from musl-1.2.5/src/signal/kill.c

nums :: import "libc/include/syscall"
sc :: import "libc/src/syscall/syscall"

kill :: proc(pid: alltypes.pid_t, sig: i32) i32 {
    r := sc.syscall2_ret(nums.SYS_kill.(i64), pid.(i64), sig.(i64))
    return r.(i32)
}
