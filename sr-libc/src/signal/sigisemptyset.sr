package libc

// Derived from musl-1.2.5/src/signal/sigisemptyset.c

sigisemptyset :: proc(set: *const alltypes.sigset_t) i32 {
    if set == null { return 1 }
    i: usize = 0
    while i < 16 {
        if set.__bits[i] != 0 { return 0 }
        i += 1
    }
    return 1
}
