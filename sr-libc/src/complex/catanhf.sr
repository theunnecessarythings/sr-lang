package libc

// Derived from musl-1.2.5/src/complex/catanhf.c

impl :: import "libc/src/complex/complex_impl"

catanhf :: proc(z: complex(f32)) complex(f32) {
    z = catanf(impl.cmplxf(-z.imag, z.real))
    return impl.cmplxf(z.imag, -z.real)
}
