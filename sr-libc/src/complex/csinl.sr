package libc

// Derived from musl-1.2.5/src/complex/csinl.c

impl :: import "libc/src/complex/complex_impl"

csinl :: proc(z: complex(f128)) complex(f128) {
    z = csinhl(impl.cmplxl(-z.imag, z.real))
    return impl.cmplxl(z.imag, -z.real)
}
