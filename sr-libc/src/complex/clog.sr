package libc

// Derived from musl-1.2.5/src/complex/clog.c

impl :: import "libc/src/complex/complex_impl"

clog :: proc(z: complex(f64)) complex(f64) {
    r := cabs(z)
    phi := carg(z)
    return impl.cmplx(log(r), phi)
}
