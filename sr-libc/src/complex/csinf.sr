package libc

// Derived from musl-1.2.5/src/complex/csinf.c

impl :: import "libc/src/complex/complex_impl"

csinf :: proc(z: complex(f32)) complex(f32) {
    z = csinhf(impl.cmplxf(-z.imag, z.real))
    return impl.cmplxf(z.imag, -z.real)
}
