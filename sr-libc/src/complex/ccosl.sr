package libc

// Derived from musl-1.2.5/src/complex/ccosl.c

impl :: import "libc/src/complex/complex_impl"

ccosl :: proc(z: complex(f128)) complex(f128) {
    return ccoshl(impl.cmplxl(-z.imag, z.real))
}
