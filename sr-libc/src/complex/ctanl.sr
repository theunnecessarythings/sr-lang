package libc

// Derived from musl-1.2.5/src/complex/ctanl.c

impl :: import "libc/src/complex/complex_impl"

ctanl :: proc(z: complex(f128)) complex(f128) {
    z = ctanhl(impl.cmplxl(-z.imag, z.real))
    return impl.cmplxl(z.imag, -z.real)
}
