package libc

// Derived from musl-1.2.5/src/complex/casinh.c

impl :: import "libc/src/complex/complex_impl"

casinh :: proc(z: complex(f64)) complex(f64) {
    z = casin(impl.cmplx(-z.imag, z.real))
    return impl.cmplx(z.imag, -z.real)
}
