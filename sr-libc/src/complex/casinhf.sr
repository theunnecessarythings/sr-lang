package libc

// Derived from musl-1.2.5/src/complex/casinhf.c

impl :: import "libc/src/complex/complex_impl"

casinhf :: proc(z: complex(f32)) complex(f32) {
    z = casinf(impl.cmplxf(-z.imag, z.real))
    return impl.cmplxf(z.imag, -z.real)
}
