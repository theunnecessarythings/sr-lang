package libc

// Derived from musl-1.2.5/src/linux/setfsgid.c

sc :: import "libc/src/syscall/syscall"
nums :: import "libc/include/syscall"

setfsgid :: proc(gid: gid_t) i32 {
    r := sc.syscall1_ret(nums.SYS_setfsgid.(i64), gid.(i64))
    return r.(i32)
}
