package libc

// Derived from musl-1.2.5/src/process/execve.c

syscall :: import "libc/src/syscall"

SYS_execve :: 59

execve :: proc(path: *const u8, argv: **u8, envp: **u8) i32 {
    return syscall.syscall3(SYS_execve, path.(u64), argv.(u64), envp.(u64)).(i32)
}
