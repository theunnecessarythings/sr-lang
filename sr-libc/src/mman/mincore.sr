package libc

// Derived from musl-1.2.5/src/mman/mincore.c

syscall :: import "libc/src/syscall"

SYS_mincore :: 27

mincore :: proc(addr: *void, len: usize, vec: *u8) i32 {
    return syscall.syscall3(SYS_mincore, addr.(u64), len, vec.(u64)).(i32)
}
