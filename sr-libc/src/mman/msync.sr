package libc

// Derived from musl-1.2.5/src/mman/msync.c

syscall :: import "libc/src/syscall"

SYS_msync :: 26

msync :: proc(start: *void, len: usize, flags: i32) i32 {
    return syscall.syscall3(SYS_msync, start.(u64), len, flags.(u64)).(i32)
}
