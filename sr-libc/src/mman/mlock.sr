package libc

// Derived from musl-1.2.5/src/mman/mlock.c

syscall :: import "libc/src/syscall"

SYS_mlock :: 149

mlock :: proc(addr: *const void, len: usize) i32 {
    return syscall.syscall2(SYS_mlock, addr.(u64), len).(i32)
}
