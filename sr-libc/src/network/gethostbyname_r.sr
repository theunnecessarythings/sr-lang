package libc

// Derived from musl-1.2.5/src/network/gethostbyname_r.c

gethostbyname_r :: proc(name: *const u8, h: *hostent, buf: *u8, buflen: usize, res: **hostent, err: *i32) i32 {
    return gethostbyname2_r(name, AF_INET, h, buf, buflen, res, err)
}
