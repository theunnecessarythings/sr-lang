package main

io  :: import "std/io"
env :: import "std/env"

main :: proc() {
    maybe_home := env.get("HOME")
    if maybe_home != null {
        home := maybe_home?
        io.print("HOME=", 0)
        io.print("%s\n", home)
    } else {
        io.print("HOME not set\n", 0)
    }
}
