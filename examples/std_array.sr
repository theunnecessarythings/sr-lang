package main

// Example: arrays (runnable subset)

io :: import "std/io"

main :: proc() {
    nums: [3]i32 = [10, 20, 30]
    io.println("len=%d", nums.len)
    io.println("first=%d", nums[0])
    io.println("second=%d", nums[1])
}
