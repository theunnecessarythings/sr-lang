package main

// Example: map-like behavior (runnable subset)

io :: import "std/io"

main :: proc() {
    keys: [2]string = ["a", "b"]
    values: [2]i64 = [100, 200]

    io.println("a=%d", values[0])

    removed := true
    if removed {
        io.println("b missing", 0)
    }
}
