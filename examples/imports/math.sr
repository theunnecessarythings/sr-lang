package main

// Standalone runner for the math module.

math :: import "math_lib"
io :: import "std/io"

main :: proc() {
    v1 := math.Vector{ x: 1.0, y: 2.0 }
    v2 := math.Vector{ x: 3.0, y: 4.0 }
    r := math.add(v1, v2)
    io.println("sum: %f, %f", (r.x, r.y))
}
