package math

// examples/imports/math.sr
// A simple "library" file to be imported.

// A public struct type.
// Assumes `pub` makes this definition available to other modules.
Vector :: struct {
    pub x: f64,
    pub y: f64,
}

// A public helper function.
add :: proc(a: Vector, b: Vector) Vector {
    return Vector{ x: a.x + b.x, y: a.y + b.y };
}
